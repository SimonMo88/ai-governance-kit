# AI governance history

This directory stores repository-local versions of AI governance documents so an
installation or upgrade can be reviewed and reverted independently of unrelated
code changes.

- `state.md` identifies the active governance version and authority map.
- `versions/` contains immutable pre-change snapshots and manifests.

Snapshots contain governance files only. They are not general repository backups
and do not replace version control.
